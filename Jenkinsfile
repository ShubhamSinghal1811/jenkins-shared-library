@Library('jenkins-shared-library')_

pipeline{
    agent any
    stages{
        stage('demo'){
               steps{
                    calculator("10","2","+") 
               }           
        }
    }
}
