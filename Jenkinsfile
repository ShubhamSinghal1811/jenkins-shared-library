@Library('jenkins-shared-library')_

pipeline{
    agent any
    stages{
        stage('demo'){
               steps{
                    calculator("$var1","$var2","$ch") 
               }           
        }
    }
}